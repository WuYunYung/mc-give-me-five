import { View } from "@tarojs/components";

export default function () {
	const renderCard = () => {
		return <View className="bg-white"></View>;
	};

	return (
		<View className="flex flex-col p-4 bg-rose">
			<View></View>
		</View>
	);
}
